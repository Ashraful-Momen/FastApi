Darun 👍 তুমি যেহেতু Laravel + Django জানো, আমি **SQLModel + FastAPI** একদম **zero → production style** এ শেখাচ্ছি — Laravel এর সাথে মিল দেখিয়ে।

> আমরা ব্যবহার করব:
> **FastAPI + SQLModel + Alembic + PostgreSQL**

SQLModel = **Pydantic + SQLAlchemy ORM**
মানে: validation + ORM একসাথে (Eloquent + FormRequest mix)

---

# STEP 1: Project Setup

```bash
mkdir fastapi_sqlmodel
cd fastapi_sqlmodel

python -m venv venv
source venv/bin/activate

pip install fastapi sqlmodel psycopg2-binary alembic uvicorn
```

---

# STEP 2: PostgreSQL DB

ধরি DB:

```sql
CREATE DATABASE fastapi_db;
```

---

# STEP 3: Database Connection (Laravel config/database.php)

```python
# app/db.py
from sqlmodel import SQLModel, create_engine, Session

DATABASE_URL = "postgresql://postgres:password@localhost:5432/fastapi_db"

engine = create_engine(DATABASE_URL, echo=True)

def get_session():
    with Session(engine) as session:
        yield session
```

---

# STEP 4: Model (Laravel Eloquent style)

```python
# app/models/user.py
from sqlmodel import SQLModel, Field
from typing import Optional

class User(SQLModel, table=True):
    __tablename__ = "users"

    id: Optional[int] = Field(default=None, primary_key=True)
    name: str
    email: str = Field(index=True, unique=True)
```

---

# STEP 5: Register Models

```python
# app/models/__init__.py
from .user import User
```

---

# STEP 6: Alembic Migration

```bash
alembic init migrations
```

Edit `alembic.ini`

```ini
sqlalchemy.url = postgresql://postgres:password@localhost/fastapi_db
```

Edit `migrations/env.py`

```python
from app.db import engine
from app.models import User
from sqlmodel import SQLModel

target_metadata = SQLModel.metadata
```

Now:

```bash
alembic revision --autogenerate -m "create users"
alembic upgrade head
```

---

# STEP 7: FastAPI App

```python
# main.py
from fastapi import FastAPI, Depends
from sqlmodel import Session, select
from app.db import get_session
from app.models.user import User

app = FastAPI()

@app.post("/users")
def create_user(user: User, session: Session = Depends(get_session)):
    session.add(user)
    session.commit()
    session.refresh(user)
    return user

@app.get("/users")
def get_users(session: Session = Depends(get_session)):
    return session.exec(select(User)).all()
```

Run:

```bash
uvicorn main:app --reload
```

---

# Laravel ↔ SQLModel mapping

| Laravel       | SQLModel            |
| ------------- | ------------------- |
| Model         | SQLModel            |
| fillable      | Pydantic fields     |
| migrate       | Alembic             |
| User::all()   | select(User)        |
| User::find(1) | session.get(User,1) |
| softDeletes   | deleted_at mixin    |

---

If তুমি চাও, next step এ দেখাবো:

* Relationship (hasMany)
* Auth user model
* Soft delete
* Pagination
* Async SQLModel version
